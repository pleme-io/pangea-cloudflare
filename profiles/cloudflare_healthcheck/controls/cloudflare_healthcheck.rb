# Code generated by pangea-forge.
# InSpec profile for verifying cloudflare_healthcheck infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set CLOUDFLARE_TOKEN and CLOUDFLARE_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/cloudflare_healthcheck/ -t cloudflare://

control 'cloudflare_healthcheck-exists' do
  impact 1.0
  title 'Manages a cloudflare_healthcheck resource'
  desc 'Verify that the cloudflare_healthcheck resource exists and has expected properties'

  describe cloudflare_healthcheck(name: attribute('id')) do
    it { should exist }
    its('address') { should_not be_nil }
    its('name') { should_not be_nil }
    its('zone_id') { should_not be_nil }
  end
end
