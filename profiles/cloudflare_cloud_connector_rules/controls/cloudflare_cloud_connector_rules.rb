# Code generated by pangea-forge.
# InSpec profile for verifying cloudflare_cloud_connector_rules infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set CLOUDFLARE_TOKEN and CLOUDFLARE_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/cloudflare_cloud_connector_rules/ -t cloudflare://

control 'cloudflare_cloud_connector_rules-exists' do
  impact 1.0
  title 'Manages a cloudflare_cloud_connector_rules resource'
  desc 'Verify that the cloudflare_cloud_connector_rules resource exists and has expected properties'

  describe cloudflare_cloud_connector_rules(name: attribute('id')) do
    it { should exist }
    its('zone_id') { should_not be_nil }
  end
end
